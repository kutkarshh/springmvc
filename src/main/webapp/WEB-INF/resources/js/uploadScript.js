// show a popup message with a button to redirect to homepage
if (confirm("User created successfully!")) {
	window.location.href = "gallery"; // redirect to homepage
} else {
	window.location.href = "gallery";
}